#include <stdio.h>

/**
  * main - Prints alphabet in lowercase
  *
  * Return: Always 0 (Success)
  */
void print_alphabet_x10(void);
int main(void)
{
	int ch;
	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	 putchar('\n');

	return (0);
}

void print_alphabet_x10(void)
{
	int alphabet;
}
